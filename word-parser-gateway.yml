---
spring:
  profiles: word-parser-gateway

eureka:
  client:
    serviceUrl:
      defaultZone: http://157.230.27.96:8010/eureka/

# zuul:
#   prefix: /services
zuul:
  ignoredServices: '*'
  routes:
    word-parser:
      path: /parser-api/**
      serviceId: word-parser-service
      stripPrefix: false

